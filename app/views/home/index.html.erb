<br/>
<div class="jumbotron text-center">
  <h1>MAWiki</h1>
  <p>Your number one source for Wiki creation.</p>
  <p>
    <% if current_user == nil %>
		  <%= link_to "Sign Up Today!", new_plans_path, class: 'btn btn-primary btn-lg' %>
    <% elsif current_user && current_user.role != 'premium' %>
      You are a <span class="text-warning">basic member</span> <%= link_to "Upgrade Today!", new_plans_path, class: 'btn btn-primary btn-lg' %>
      <% elsif current_user && current_user.role == 'premium' %>
      <span class="text-success">Congratulations!</span> You are a <span class="text-success">Premium</span> Member.</p>
    <% end %>
  </p>
</div>
<div>
  <div class="row">
    <div class="col-md-4 col-md-offset-2">
      <h2 class="text-center"><span class="text-warning">Members</span> can:</h2><br>
      <h4 class="text-center">Create unlimited public wikis!</h4><br>
      
    </div>
    <div class="col-md-4">
      <h2 class="text-center"><span class="text-success">Premium</span> Members get to:</h2><br>
      <h4 class="text-center">Create private wikis with collaborators!</h4><br>

      
    </div>
  </div>
</div> 

